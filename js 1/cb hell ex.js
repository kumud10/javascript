//Example , using functions in callback hell

function setInfo(name) {
    address(myAddress); {
        officeAdress(myOfficeAddress); {
            telephoneNumber(myTelephoneNumber); {
                nextOfKin(myNextOfKin); {
                    console.log('done'); 
                }
            }
        }
    }
}